spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/stage_db_v1}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:azerty123.}
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: none
        format_sql: true
        jdbc:
          time_zone: UTC
    show-sql: true
    database: postgresql
    generate-ddl: true
    open-in-view: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  liquibase:
    change-log: classpath:db/db.changelog-master.xml
    contexts: dev, faker
    enabled: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: cartecommerce67@gmail.com
    password: zejuaaoxcijqwodz
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  output:
    ansi:
      enabled: always

  security:
    cors:
      origin: http://localhost:5173
      max-age: 3600

  front:
    url: http://localhost:5173


server:
  port: 8089
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
